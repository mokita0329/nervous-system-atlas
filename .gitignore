# private source material and derived corpus — never commit
source/
reference/

# pipeline downloads / intermediates
pipeline/raw/
pipeline/work/
pipeline/qa/*.png
# atlas-subject renders: an individual's scan, before and after defacing
pipeline/qa/subjects/
blender/work/

# generated data (regenerable from pipeline + content, or `npm run data`)
public/data/
# the release archive `npm run data` fetches and scripts/release-data.sh builds
atlas-data-*.tar.gz

# environments
.venv*/
node_modules/
dist/
*.pyc
__pycache__/
.DS_Store
test-results/
playwright-report/
blender/.venv/
dist-private/
pipeline/qa/fudan_spine/
pipeline/qa/cord_public/
qa/shots/
pipeline/qa/report.json
.claude/diagrams/
